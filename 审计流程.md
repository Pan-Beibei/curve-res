针对自定义 DeFi 经济合约的实战型审计 SOP，将整个流程凝炼为 **7 阶段工程化工作流**：

**1. 威胁建模与架构破译（Threat Modeling）**

- **做什么：** 不看代码细节，先看文档与白皮书，画出资金流动图（Money Flow）与状态机转移图（State Machine）。标记核心攻击面：外部入口、权限函数、Oracle 依赖、闪电贷交互点。
- **核心产出：** 系统威胁模型（攻击者视角下的资金流向与漏洞假设）。
- **工具与能力：** Excalidraw / Mermaid, Slither (生成控制流图 CFG)；具备 DeFi 组合性攻击思维与架构建模能力。

**2. EVM 兼容影子账本构建（Shadow Ledger）**

- **做什么：** 用 Python 或 SageMath 重构套逻辑，**严格弃用浮点数，完全模拟 EVM 的离散整数算术**（强制整数除法截断 `//`、定点数 WAD 精度、明确向上/向下取整方向）。该模型抛弃一切 Solidity 汇编优化与存储结构，仅作为一份**无 Gas 优化污染的“绝对正确 EVM 账本”**。
- **核心产出：** 具备 EVM 精度舍入特性的离散数学参考标准（Ground Truth）。
- **工具与能力：** Python, SageMath；具备定点数数学与 EVM 整数截断算术的建模能力。

**3. 代码审查与绝对不变量提取（Code Review & Invariant Mining）**

- **做什么：** 逐行审查 Solidity/Yul 源码，排查重入、权限缺失、内存污染及位运算风险。更重要的是：**提炼出系统绝对不能被打破的数学规则（Invariants）**（如：`Virtual Price 必须单调递增`、`Vault 实际余额 >= 账面总份额`）。
- **核心产出：** 漏洞清单（Draft）与系统的形式化不变量规则集。
- **工具与能力：** Solidity Visual Developer, VS Code；具备 Yul/Assembly 汇编穿透与位掩码解构能力。

**4. 形式化验证与约束求解（Formal Verification）**

- **做什么：** 将提取的不变量转化为形式化规范（如 CVL），利用 SMT 求解器在代数空间内穷尽验证单合约函数级逻辑。若存在逻辑死角，求解器会直接抛出数学反例（Counterexample）。
- **核心产出：** 逻辑反例或不变量的单函数数学证明。
- **工具与能力：** Certora Prover, Halmos；具备 CVL 规范编写与 SMT 报错解读能力。

**5. 双机对撞与差异化测试（Differential Fuzzing）**

- **做什么：** 将 Solidity 链上合约与 Python 影子账本挂载至同一个引擎，并发注入 10,000+ 步随机的多角色交易序列（`存 -> 砸盘 -> 闪电贷 -> 提现`）。每一步实时对账，捕捉任何 1-wei 的精度溢出、状态漂移或微量套利漏洞。
- **核心产出：** 包含引发状态偏差的完整交易轨迹（Trace Dump）。
- **工具与能力：** Foundry (Forge Invariants), Medusa, Echidna, Hypothesis；具备属性模糊测试与自动化对撞脚本搭建能力。

**6. 攻击链 PoC 武器化（PoC Weaponization）**

- **做什么：** 将 Fuzzing 和形式化验证打出的 Trace Dump，在本地分叉网络（Mainnet Fork）中还原为真实的攻击脚本。引入 MEV 抢跑、闪电贷成本与 Gas 费计算，验证其商业获利可行性。
- **核心产出：** 包含完整攻击路径、可直接抽干资金或造成 DoS 的 Solidity PoC。
- **工具与能力：** Foundry (Chaintester/Forking), Tenderly；具备套利机器人与黑客攻击链编写能力。

**7. 修复复核与闭环交付（Re-Audit & Final Report）**

- **做什么：** 提交包含漏洞分析与 PoC 的初版报告；对开发者提交的补丁（PR）逐行复核，确保修复方案有效且没有破坏第二步的影子账本逻辑。最后签署出具最终审计报告。
- **核心产出：** 盖章签署的 Final Audit Report。
- **工具与能力：** Git Diff, Pull Request Review；具备安全工程二次影响分析能力。

---
