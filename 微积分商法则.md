**核心结论：这直接套用了微积分中的“商法则”（Quotient Rule）。**

由于 $M$ 和 $K_0$ 都是包含底层变量 $D$ 的函数，在求导时，两个函数的“相除”必须使用专门的除法求导公式：

$$\left( \frac{u}{v} \right)' = \frac{u'v - uv'}{v^2}$$

### 公式代入步骤

- **分子定义：** 设 $u = M$，则其导数 $u' = M'$。
- **分母定义：** 设 $v = K_0$，则其导数 $v' = K_0'$。
- **直接套用公式：**
  将 $u$ 和 $v$ 代入商法则公式中，直接得出 $\left( \frac{M}{K_0} \right)' = \frac{M' K_0 - M K_0'}{K_0^2}$。
- **拼接负号：**
  原方程中这一项前面带有一个负号（$- \frac{M}{K_0}$），所以直接在得出的导数前加上负号，即为 $- \frac{M' K_0 - M K_0'}{K_0^2}$。
