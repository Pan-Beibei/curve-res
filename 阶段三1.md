非常好！我们要保持这种“不放过任何一个细节”的极客精神。

现在我们承接上一阶段的结尾，正式推导卡尔丹公式的最终形态，并借此踏入阶段三（多解与复数域）的大门。

在上一阶段，我们构造出了一个关于 $u^3$ 和 $v^3$ 的一元二次方程（二次预解式）：

$$Z^2 + qZ - \frac{p^3}{27} = 0$$

### 第一步：套用求根公式解出 $Z$

这是一个标准的 $aZ^2 + bZ + c = 0$ 的方程。
我们直接把它的系数列出来：

- $a = 1$
- $b = q$
- $c = -\frac{p^3}{27}$

初中的一元二次求根公式是：$Z = \frac{-b \pm \sqrt{b^2 - 4ac}}{2a}$
我们把上面的系数老老实实代进去：

$$Z = \frac{-q \pm \sqrt{q^2 - 4 \cdot 1 \cdot \left(-\frac{p^3}{27}\right)}}{2 \cdot 1}$$

化简根号里面的负负得正：

$$Z = \frac{-q \pm \sqrt{q^2 + \frac{4p^3}{27}}}{2}$$

### 第二步：极其关键的代数化简（凑平方和立方）

为了让公式更加对称、优雅，我们要把分母上的 $2$ “塞”进根号里面去。
我们知道，外面的 $\frac{1}{2}$ 放到平方根里面，就变成了 $\frac{1}{4}$。

所以，我们把公式拆开写：

$$Z = -\frac{q}{2} \pm \frac{1}{2}\sqrt{q^2 + \frac{4p^3}{27}}$$

$$Z = -\frac{q}{2} \pm \sqrt{\frac{1}{4} \cdot \left(q^2 + \frac{4p^3}{27}\right)}$$

把 $\frac{1}{4}$ 乘进根号里的每一项：

$$Z = -\frac{q}{2} \pm \sqrt{\frac{q^2}{4} + \frac{4p^3}{27 \times 4}}$$

化简第二项（分子分母的 4 约掉），然后把分数分别写成平方和立方的形式：

$$Z = -\frac{q}{2} \pm \sqrt{\left(\frac{q}{2}\right)^2 + \left(\frac{p}{3}\right)^3}$$

### 第三步：见证卡尔丹公式的诞生

还记得 $Z$ 是什么吗？$Z$ 代表的是二次方程的两个根，也就是我们之前假设的 $u^3$ 和 $v^3$！
既然有两个解（一个加号，一个减号），我们自然可以分配给 $u^3$ 和 $v^3$：

$$u^3 = -\frac{q}{2} + \sqrt{\left(\frac{q}{2}\right)^2 + \left(\frac{p}{3}\right)^3}$$

$$v^3 = -\frac{q}{2} - \sqrt{\left(\frac{q}{2}\right)^2 + \left(\frac{p}{3}\right)^3}$$

接下来，对等式两边**开立方根**，我们就能得到 $u$ 和 $v$ 的真面目：

$$u = \sqrt[3]{-\frac{q}{2} + \sqrt{\left(\frac{q}{2}\right)^2 + \left(\frac{p}{3}\right)^3}}$$

$$v = \sqrt[3]{-\frac{q}{2} - \sqrt{\left(\frac{q}{2}\right)^2 + \left(\frac{p}{3}\right)^3}}$$

最后一步！还记得我们最初的假设吗？**$t = u + v$**！
把上面两坨巨大的式子加起来，我们就得到了**缺项三次方程 $t^3 + pt + q = 0$ 的最终解析解**：

$$t = \sqrt[3]{-\frac{q}{2} + \sqrt{\left(\frac{q}{2}\right)^2 + \left(\frac{p}{3}\right)^3}} + \sqrt[3]{-\frac{q}{2} - \sqrt{\left(\frac{q}{2}\right)^2 + \left(\frac{p}{3}\right)^3}}$$

这就是横空出世、震撼数学史的**卡尔丹公式（Cardano's Formula）**！

_(注：算出 $t$ 之后，只需要代回我们第一阶段的 $x = t - \frac{B}{3}$，就能得到最原始的三次方程的根 $x$ 了。)_

---

### 第四步：提取判别式 $\Delta$ (进入阶段三的钥匙)

盯着上面那个庞大的公式，你会发现里面有一块完全一样的核心部分，藏在最深层的平方根下面。
数学家把它提取出来，定义为**判别式 $\Delta$ (Discriminant)**：

$$\Delta = \left(\frac{q}{2}\right)^2 + \left(\frac{p}{3}\right)^3$$

这块 $\Delta$ 决定了三次方程的生杀大权，也直接决定了智能合约会不会崩溃：

- 如果 $\Delta > 0$，平方根里面是正数，天下太平。
- 如果 $\Delta < 0$，平方根里面是负数，我们需要对负数开平方，这就产生了一个叫虚数（$i$）的怪物。

---

**抛出致命问题**

代数推导到这里，完美无瑕。但是，这里隐藏着一个巨大的逻辑漏洞，这也是所有初学者都会困惑的地方，更是高阶审计师必须跨越的鸿沟：

**一元三次方程，无论如何都必须有三个根！**
可是按照我们刚才的公式 $t = u + v$，我们只算出了**一个**实数结果！**另外两个隐藏的根去哪了？**

面对这“失踪的两个根”，我们接下来是先去复数域里把它们找出来（学习三次单位根 $\omega$），还是先看看当 $\Delta < 0$ 时，这个完美的公式是如何让 EVM 陷入死局的？
