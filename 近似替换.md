这绝对是整个推导中最具“黑客精神”的一步！Michael Egorov 在这里做了一个极其大胆的工程化裁剪。

我们分两部分来拆解：首先看“为什么可以这么替”**（数学原理），然后再看**“具体是怎么算出来的”（代数化简）。

### 第一部分：为什么可以做“近似替换”？

我们在用牛顿迭代法找 $F(D) = 0$ 的解。牛顿法的本质是：顺着切线往下走，每一次算出来的新 $D$，都会让 $F(D)$ 离 0 更近。

在迭代的过程中，我们其实已经**非常接近** $F(D) = 0$ 了。
如果我们强行**假设等式已经成立**，也就是令：

$$S - D + M \left( 1 - \frac{1}{K_0} \right) = 0$$

通过移项，把含有 $K_0$ 的那一项留在左边，其他移到右边：

$$M \left( 1 - \frac{1}{K_0} \right) = D - S$$

等式两边同除以 $M$：

$$1 - \frac{1}{K_0} = \frac{D - S}{M}$$

**Egorov 的狡猾之处在于：** 他知道在这个复杂的导数 $F'(D)$ 中，如果老老实实去算 $1 - \frac{1}{K_0}$，既费 Gas 又容易丢失精度。既然我们每一步都在逼近 $F(D) = 0$，不如直接把 $\frac{D - S}{M}$ 塞进导数公式里。
这在数学上叫作“拟牛顿法（Quasi-Newton Method）”——用一个极其接近真实导数的“近似导数”来指明方向，虽然每次走的步子可能差了一丝丝，但最终依然能完美射中靶心，而且省下了大量的计算资源！

---

### 第二部分：代数化简的“爽感”时刻

现在我们来看看这个替换是怎么让复杂公式瞬间瘦身的。

我们在上一步得到的总导数公式是：

$$F'(D) = -1 + M' \left( 1 - \frac{1}{K_0} \right) + \frac{M \cdot K_0'}{K_0^2}$$

我们重点对付后面这两大块。

#### 1. 拆解中间那一块：$M' \left( 1 - \frac{1}{K_0} \right)$

之前我们已经极其辛苦地推导出了 $M'$ 的等价形态：

$$M' = \frac{M}{D} (1 + \text{mul2})$$

现在，我们用刚才的近似公式，把 $(1 - \frac{1}{K_0})$ 替换成 $\frac{D - S}{M}$：

$$M' \left( 1 - \frac{1}{K_0} \right) = \underbrace{ \frac{M}{D} (1 + \text{mul2}) }_{M'} \cdot \underbrace{ \frac{D - S}{M} }_{\left( 1 - \frac{1}{K_0} \right)}$$

**魔法出现了！**
你仔细看这个乘法：左边有一个在分子上的 $M$，右边有一个在分母上的 $M$。
**它们直接互相约掉了！**

约掉 $M$ 之后，式子瞬间变得极其清爽：

$$= \frac{1}{D} (1 + \text{mul2}) \cdot (D - S) = \frac{D - S}{D} (1 + \text{mul2})$$

这就是导数中间那一项的最终形态。

#### 2. 拆解最后那一块：$\frac{M \cdot K_0'}{K_0^2}$

这一项没有参与“近似替换”，我们只需要老老实实把 $K_0'$ 代进去化简。
在推导的第八步，我们算过 $K_0$ 对 $D$ 的导数：

$$K_0' = -N \frac{K_0}{D}$$

我们把这个 $K_0'$ 代入到最后一个分数中：

$$\frac{M \cdot K_0'}{K_0^2} = \frac{M \cdot \left( -N \frac{K_0}{D} \right)}{K_0^2}$$

把分子上的负号提取到最前面，并将分子里的 $K_0$ 和分母里的 $K_0^2$ 约掉一个：

$$= -\frac{M \cdot N \cdot K_0}{D \cdot K_0^2} = -\frac{M \cdot N}{D \cdot K_0}$$

---

### 第三步：大满贯拼装

现在，我们把化简好的两块，重新放回总导数 $F'(D)$ 中：

- 第一项：$-1$（原封不动）
- 第二项：替换成了 $\frac{D - S}{D} (1 + \text{mul2})$
- 第三项：化简成了 $-\frac{M \cdot N}{D \cdot K_0}$

拼在一起，一字不差就是你提问中的那行终极公式：

$$F'(D) = -1 + \frac{D - S}{D} (1 + \text{mul2}) - \frac{M \cdot N}{D \cdot K_0}$$

这步“近似替换”不仅没有破坏数学的收敛性，反而通过代数相消（消灭了烦人的 $M$），为以太坊虚拟机节省了巨量的 Gas 费。可以说是纯粹的工程学艺术。
