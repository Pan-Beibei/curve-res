画出状态机转移图：明确一笔贷款从 Submitted 到 Accepted，再到 Repaid 或 Liquidated 的所有状态转换路径，以及触发这些转换的必要条件 [行业通用常识]。

画出资金流向图（Money Flow）：借款人的抵押品锁死在哪里？出借人的资金流向了谁？利息累加逻辑在哪个合约中记账？ [推论演绎]。

提取必须死守的不变量：在阅读代码逻辑前，先通过宏观设计推断系统不能被打破的数学底线（例如：清算后返回给借款人的剩余资产必须严格等于抵押资产减去坏账及罚息） [推论演绎]。

为了真正打磨你这套 7 阶段 SOP，你现在必须强迫自己执行以下约束：

封锁实现层：在画出完整的贷款状态生命周期图（如 Pending -> Accepted -> Repaid / Liquidated）以及资金流向图之前，禁止阅读函数体内的任何具体逻辑 [推论演绎]。

确立边界：基于官方文档和接口声明，写下绝对不能被打破的 3 到 5 个系统级数学不变量（例如：放款人提取的资金总量绝不能大于借款人偿还的本息之和） [行业通用常识]。

对比验证：进入 SOP 第 3 步代码审查时，你的唯一目标是去代码中寻找能够击穿上述不变量的执行路径，而不是漫无目的地“挨个检查接口是否存在问题” [推论演绎]。
