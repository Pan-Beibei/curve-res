非常完美。面对那“失踪的两个根”，我们绝不糊弄，直接迎难而上。

欢迎来到**阶段三：多解与复数域（寻找失踪的根）**。

在上一阶段，我们得出了 $u$ 和 $v$ 的表达式。当我们在计算 $u = \sqrt[3]{\dots}$ 时，我们的直觉告诉我们：一个数开三次方，结果只有一个。比如 $\sqrt[3]{8} = 2$。

但在更广阔的数学世界（复数域）里，**任何一个非零数字，都必然有三个立方根！** 刚才我们只找到了那个最明显的“实数根”，而漏掉了两个“复数根”。

为了把它们找出来，请紧跟我的推导：

### 第一步：解剖最简单的方程 $x^3 = 1$

要想知道任意数字的三个立方根怎么找，我们先从最简单的数字 $1$ 开始。
我们要解这个方程：

$$x^3 = 1$$

移项得到：

$$x^3 - 1 = 0$$

根据初中学过的**立方差公式** $a^3 - b^3 = (a - b)(a^2 + ab + b^2)$，我们可以把上面的方程因式分解：

$$(x - 1)(x^2 + x + 1) = 0$$

要想让这个乘积等于 0，要么左边括号等于 0，要么右边括号等于 0。

- **情况 1**：$x - 1 = 0$ ，解得 $x = 1$。这就是我们都认识的那个唯一的“实数根”。
- **情况 2**：$x^2 + x + 1 = 0$。重点来了，我们要对它套用一元二次求根公式（$a=1, b=1, c=1$）！

$$x = \frac{-1 \pm \sqrt{1^2 - 4 \cdot 1 \cdot 1}}{2}$$

$$x = \frac{-1 \pm \sqrt{-3}}{2}$$

因为根号里面是负数，我们引入虚数单位 $i$（定义 $i^2 = -1$，所以 $\sqrt{-3} = \sqrt{3 \cdot -1} = \sqrt{3}i$）。
于是，另外两个神秘的根出现了：

$$x = -\frac{1}{2} + \frac{\sqrt{3}}{2}i$$

$$x = -\frac{1}{2} - \frac{\sqrt{3}}{2}i$$

数学家给第一个复数根起了一个专属名字，叫做 **$\omega$ (Omega)**。
更神奇的是，如果你把 $\omega$ 平方（你可以自己在草稿纸上算一下 $(\omega)^2$），你会发现结果刚好等于第二个复数根！所以第二个根被记为 **$\omega^2$**。

**核心结论：数字 $1$ 的三个立方根分别是：$1$、$\omega$、$\omega^2$。**（并且因为它们是 1 的立方根，所以 $\omega^3 = 1$）。

### 第二步：推演 $u$ 和 $v$ 的全貌

既然 $1$ 有三个立方根，那么任何数字开立方都有三个结果。
假设我们在上一阶段算出来的那个普通的、能按计算器按出来的实数立方根分别叫 $u_1$ 和 $v_1$。

那么，$u^3$ 的完整三个根其实是：

- 第一个根：$u_1$
- 第二个根：$u_1 \cdot \omega$
- 第三个根：$u_1 \cdot \omega^2$

同理，$v^3$ 的完整三个根是：

- 第一个根：$v_1$
- 第二个根：$v_1 \cdot \omega$
- 第三个根：$v_1 \cdot \omega^2$

### 第三步：强制配对（回忆韦达替换的紧箍咒）

既然 $t = u + v$，而 $u$ 和 $v$ 各有 3 种形态，是不是意味着 $t$ 有 $3 \times 3 = 9$ 个解呢？
**绝对不是！** 三次方程只能有 3 个根。那多出来的组合是怎么被淘汰的？

这就要召唤出我们在上一阶段“强行附加”的那个降维条件了！还记得吗？
我们当时规定：

$$3uv + p = 0$$

也就是：

$$uv = -\frac{p}{3}$$

注意看：$p$ 是我们原始方程的系数，它是一个**实数**。这意味着，**你挑出来的 $u$ 和 $v$，乘起来必须是一个没有任何虚数 $i$ 的纯实数！**

我们来测试一下配对：

- **配对 A**：选 $u_1$ 和 $v_1$。相乘是 $u_1 v_1$，是实数。**合格！**
- **配对 B**：选 $u_1 \omega$ 和 $v_1 \omega$。相乘是 $u_1 v_1 \omega^2$。带有虚数，**淘汰！**
- **配对 C**：选 $u_1 \omega$ 和 $v_1 \omega^2$。相乘是 $u_1 v_1 \omega^3$。因为我们证明过 $\omega^3 = 1$，所以相乘结果就是 $u_1 v_1 \cdot 1 = u_1 v_1$。是实数，**合格！**

通过这种严密的配对检查，9 种组合中只有 3 种能活下来。

### 第四步：三根归位

把合格的 $u$ 和 $v$ 加起来（$t = u + v$），我们终于找齐了缺项三次方程 $t^3 + pt + q = 0$ 的三个根：

- **真正的第一个根**：

$$t_1 = u_1 + v_1$$

- **真正的第二个根**：

$$t_2 = u_1 \omega + v_1 \omega^2$$

- **真正的第三个根**：

$$t_3 = u_1 \omega^2 + v_1 \omega$$

---

**阶段总结与终极悬念**

至此，在纯数学的领域里，卡尔丹公式已经完美无瑕。我们找齐了 3 个根，没有跳过任何一个步骤。

但作为未来的高阶审计师，你的警报雷达现在应该疯狂作响。请你仔细盯着这三个根。
我们发现，$t_2$ 和 $t_3$ 的公式里，**明晃晃地包含着 $\omega$ 和 $\omega^2$（里面有虚数 $i$）**！

**但现实世界中的流动性池代币余额（比如 ETH 的数量），绝不可能是一个带有虚数 $i$ 的复数，它必须是实数。**

那么问题来了：
既然 $t_2$ 和 $t_3$ 公式里有虚数 $i$，有没有一种极端情况，导致算出来的结果，虚数部分刚好被全部抵消掉，使得 $t_2$ 和 $t_3$ 最终变成了**真实的实数解**？

如果有，这就意味着在某些特殊的池子状态下，方程会产生**三个纯实数根**。而当这种情况发生时，我们的判别式 $\Delta$ 会作何反应？EVM 又会面临怎样的灭顶之灾？

确认你已经完全消化了这三个根的推导过程，我们就进入数学史上最著名的“不可约情形（Casus Irreducibilis）”，彻底解开 Curve V2 代码必须写 fallback 降级的根本原因。
